<header>
    <!-- Fixed navbar -->
    <nav class="navbar navbar-expand-md navbar-dark fixed-top bg-dark border-bottom">
        <a class="navbar-brand" href="{{ path('home.index') }}">Carnegie</a>
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarCollapse" aria-controls="navbarCollapse" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarCollapse">
            <ul class="navbar-nav mr-auto">
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('home.index') }}">Homepage</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('customers.index') }}">Clients</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('companies.index') }}">Companies</a>
                </li>
            </ul>
        </div>
    </nav>
</header>